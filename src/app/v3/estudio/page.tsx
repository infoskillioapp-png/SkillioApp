import StudySpace from "../_components/study-space";

// Pantalla de referencia de Skillio 3.0 — "Espacio de estudio".
// Prototipo visual con data mock para fijar el look. Sin backend todavía.
export const metadata = { title: "Skillio 3.0 · Espacio de estudio" };

export default function EstudioPage() {
  return <StudySpace />;
}
