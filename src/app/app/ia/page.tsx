// espacio-de-estudio.html — se construye en el próximo paso
export default function IAPage() {
  return (
    <div style={{ display: "grid", placeItems: "center", minHeight: "100vh", fontFamily: "var(--po)", color: "var(--ink)" }}>
      <p style={{ color: "var(--muted)" }}>Espacio de estudio — próximamente</p>
    </div>
  );
}
