import { GraciasClient } from "./_components/gracias-client";

// Pantalla de gracias post-pago (checkout embebido). Momento de celebración +
// confirmación de que el plan quedó activo, antes de entrar a la app.
export default function GraciasPage() {
  return <GraciasClient />;
}
